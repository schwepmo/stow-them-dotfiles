#!/bin/bash

if (($# == 0)); then
  echo "Usage: omarchy-launch-or-focus-firexfox-webapp [window-pattern] [url-and-flags...]"
  exit 1
fi

WINDOW_PATTERN="$1"
shift
LAUNCH_COMMAND="omarchy-launch-firefox-webapp $@"

exec omarchy-launch-or-focus "$WINDOW_PATTERN" "$LAUNCH_COMMAND"
